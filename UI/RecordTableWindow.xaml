<Window x:Class="UI.RecordTableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Таблица рекордов" Height="400" Width="500">
    <Grid>
        <ListView x:Name="ListView" ItemsSource="{Binding Table}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=DateTime}" Width="170">Дата и время получения</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Difficult}" Width="170">Сложность</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=SolutionTime}" Width="100">Время</GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
